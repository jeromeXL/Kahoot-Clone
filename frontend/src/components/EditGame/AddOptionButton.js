import React from 'react';
import Button from 'react-bootstrap/esm/Button';

export default function AddOptionButton (props) {
  return (
    <Button aria-label='Add Option Button' {...props}> Add Option </Button>
  );
}
